---
name: flutter-common
description: Использовать, когда нужно понять общий слой приложения и его части, общие для нескольких фич: UI-компоненты, утилиты, модели и контракты, хранение, сеть, платформа, навигация, запуск и сборка, оформление. Описывает на верхнем уровне, что это за части и что в каждой лежит.
type: skill
---

# Общий слой приложения

Всё, что нужно **двум и более** фичам: переиспользуемый код и инфраструктура приложения. Правило «вынести или держать локально» — архитектурное; здесь — про то, **из каких частей** состоит общий слой и зачем каждая.

Уровень намеренно верхний. Папки добавляются по мере необходимости; какая-то часть разрастётся — вынесем её в отдельный скилл, пока всё описано здесь.

## Прикладные кирпичи

- **Виджеты (widgets)** — переиспользуемые UI-компоненты: без логики конкретной фичи, настройка снаружи через параметры.
- **Утилиты (functions)** — чистые помощники и расширения; без состояния и без выхода во внешний мир.
- **Модели (models)** — типы данных для нескольких фич и единый тип ошибки `AppFailure`.
- **Контракты (domain)** — общие интерфейсы интеграций; реализация прячется за контрактом.

## Инфраструктура

Сборка приложения и выход во внешний мир.

- **Запуск (core)** — точка входа, конфигурация, сборка и связывание зависимостей.
- **Навигация (navigation)** — переходы между экранами.
- **Хранение (storage)** — локальные данные приложения.
- **Платформа (platform)** — нативные возможности устройства: разрешения, доступ к сети, информация об устройстве.
- **Сеть (http)** — выход к внешним серверам, общие перехватчики (interceptors).
- **Логирование и аналитика (logging)** — события и ошибки.
- **Оформление (theme)** — цвета, типографика, темизация.

## Границы

Общий слой **не зависит от фич** — зависимость только в одну сторону. И не обобщаем раньше времени: спорное сначала живёт в фиче, переносим сюда после реального переиспользования.
